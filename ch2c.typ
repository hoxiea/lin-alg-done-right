=== Chapter 2C
#let span = text("span")
#let dim = text("dim")

1. Show that the subspaces of $RR^2$ are precisely ${0}$, all lines in $RR^2$ containing the origin, and $RR^2$.

#pad(left: 11pt)[
]

2. Show that the subspaces of $RR^3$ are precisely ${0}$, all lines in $RR^3$ containing the origin, all planes in $RR^3$ containing the origin, and $RR^3$.

#pad(left: 11pt)[
]

3. 

